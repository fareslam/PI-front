import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.flam',
  appName: 'material-dashboard-angular',
  webDir:'dist/material-dashboard-angular',bundledWebRuntime:false
};

export default config;
